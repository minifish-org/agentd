# PUT this document to /v1/tenants/demo/agents/simple-bot.
# No persona: the native agent loop uses the host's default system prompt.
model = "standard/chat"
timeout_ms = 30000
max_steps = 16
max_tokens = 4096
